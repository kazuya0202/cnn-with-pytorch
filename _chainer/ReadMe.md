##認識実験は 未知実験 → 既知実験の順番

##誤認識画像はフォルダ名→正解 フォルダの中身→そのフォルダ名に誤認識した画像


##IsOverwriteTestData を True にするとDatasetSizeすべてが学習用画像になります(DatasetSize == TrainSize)
##テストデータを上書きするときは，上書き用フォルダのフォルダ名をクラス名に統一してください


